#container
  .flexslider
    %ul.slides
      - articles.each do |article|
        %li
          .image= image_tag article.image.url(:slider) 
          %h2.title= article.title
          .info= article.body
