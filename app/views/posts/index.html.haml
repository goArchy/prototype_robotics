%h1 Listing posts

- @posts.each do |post|
  %table
    %tr
      %td= link_to image_tag(post.image.url(:thumb)), post_path(post)
      %td= post.title
      %td= "Posted:" + "#{post.created_at.strftime("%m/%d/%Y at %I:%M%p")}"

%br
- if current_user
  = link_to 'New Post', new_post_path
