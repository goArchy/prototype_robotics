%h1.title= link_to "Projects List", projects_path

.categories
  - @projects.collect{|s| s.category }.uniq.each do |category|
    = link_to category, search_projects_path(:category => category), :class => "category"
.listing
  - @projects.each do |project|
    .display_box
      .display_image
        %center
          = link_to image_tag(project.image.url(:thumb), :class => "imgshadow"), project_path(project)
      .display_title
        %center
          =h link_to truncate(project.name, :length => 20), project

